// ignore_for_file: use_full_hex_values_for_flutter_colors

import 'package:flutter/material.dart';

class MyColor {
  static Color redColor = const Color(0xFFF10052); //just call the attribute
  static Color purpleColor = Colors.purple;
  static Color deepOrange = Colors.deepOrangeAccent;
  static Color? backgroundColor = Colors.orange[200];
  static Color? greyColor = Colors.grey[100];
}

const Color redColor = Color(0xFFF10052); //just call the name const
